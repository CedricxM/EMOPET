name: Temporary lockfile migration evidence

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  evidence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Stop marker
        run: |
          echo "This temporary workflow is intentionally non-mutating."
          echo "Do not use broad trust disablement as migration evidence."
          echo "A reviewed exact-selector migration manifest is required by #106 before lock regeneration."
