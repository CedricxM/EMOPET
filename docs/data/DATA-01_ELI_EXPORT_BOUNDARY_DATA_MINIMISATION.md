# Data minimisation

This slice does not add storage to satisfy export functionality. It exports only from data already persisted by the current backend and keeps raw high-rate streams explicitly unavailable rather than fabricating or newly retaining them.
