# EMOPET R2 open-items workspace

**Status:** `INTERNAL CONTROLLED / OPEN ITEMS / NOT RELEASED`  
**Repository base:** `c099581ff8aed1e619f72ab38898fc05833b7c66`  
**R2 checkpoint SHA-256:** `1fda848d1bb7a77513450f797171ba672f9a7fc7062b701f6af8c088ad713ce7`  
**Scope:** 63 active gates and 444 R2 reconciliation-matrix occurrences.

This folder is a quarantined GitHub working set for unresolved document-control work. Its
presence in the private repository is custody and review access only. It is not evidence of
approval, closure, release, transmission, manufacturing readiness, validation, component
selection, legal advice, funding, scientific performance or publication authority.

## Repository authority

The repository `AGENTS.md` remains authoritative for current repository behaviour and brand.
Its 2026 rebrand supersedes the historical EMOPET v2 visual authority recorded inside R2 for
repository-facing work. Exact R2 records are retained here as historical control evidence and
must not be treated as current repository brand authority.

## Included

- the exact 70-row R2 gate register and a derived 63-row active view;
- one review card for each of the 63 active/next-release-only gates;
- the exact 444-row R2 reconciliation matrix;
- exact R2 source, evidence, supersession and coverage registers;
- exact Batch 0-9 authority reports/registers and open-evidence registers available in R2;
- a 444-row source-payload reference index;
- a promotion manifest and QA report;
- a 430-file global source-corpus QA snapshot under `qa-global-2026-09-01/`.

## Excluded by control

- all 430 immutable global-source payload binaries and source documents;
- all third-party originals;
- all MOKO archives and supplier payloads;
- all historical checkpoint ZIPs and nested archives;
- all pilot/publication binaries and mass-converted outputs;
- credentials, secrets and live environment material.

The excluded items remain traceable by original path and SHA-256 in
`indexes/SOURCE_PAYLOAD_REFERENCE_INDEX.csv`. They are not silently deleted, replaced,
restyled or rehashed.

## Navigation

- `indexes/OPEN_GATE_INDEX.md` — gate counts and links;
- `gates/` — 63 category-grouped gate cards;
- `registers/` — exact and derived R2 registers;
- `batches/` — exact R2 Batch control records;
- `indexes/SOURCE_PAYLOAD_REFERENCE_INDEX.csv` — source coverage and exclusion rationale;
- `qa-global-2026-09-01/` — 430-file QA snapshot, findings and improvement guide.

Large text registers are stored as lossless newline-boundary parts. Concatenating their parts
in numeric order reproduces the recorded original size and SHA-256 in
`CHUNKED_FILE_INDEX.csv`.

## Status rules

1. Do not change `OPEN`, `HOLD`, `BLOCKED`, `PREPARED_NOT_SENT`, `PASS_WITH_NOTES` or
   `NOT RELEASED` from repository availability alone.
2. Do not infer technical, scientific, legal, financial, physical or manufacturing conclusions.
3. Do not reconstruct MOKO r1.6 from historical packages or claim that it was sent.
4. Close a gate only with its recorded authority and required evidence.
5. Generate new release-control hashes only after a separately authorised frozen payload.
