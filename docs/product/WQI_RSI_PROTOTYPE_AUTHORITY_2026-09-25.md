# WQI / RSI prototype-authority boundary

**Issue:** #91  
**Status:** `HOLD / DEMO ONLY / NO PRODUCT ENTITLEMENT AUTHORITY`  
**Date:** 2026-09-25

The current web WQI and RSI values are generated by the explicit ELI demo/mock path. They are not produced by a canonical backend ELI runtime.

## Current controlled disposition

- WQI 40/35/25 weights: `EMOPET_PROTOTYPE_CHOICE`.
- RSI 14-day comparison + 80/50 thresholds: `EMOPET_PROTOTYPE_CHOICE`.
- Neither has a recovered controlled “ELI v6 §7/§8” authority.
- Neither is authorized as a paid-tier feature while #91 remains HOLD.
- Demo rendering may remain because it is already explicitly marked `DÉMO` and sourced from the non-authoritative mock path.

Machine-readable authority:

`config/product/wqi-rsi-authority.json`

## Activation requirements

Before either feature returns to the commercial entitlement registry:

1. #91 selects/retains a controlled semantic definition;
2. weights/thresholds receive explicit provenance and validation/calibration plan;
3. #118/#124 provide a canonical runtime/API producer;
4. production clients must not fall back to mock values;
5. end-to-end tests prove version parity, missing-data abstention and boundary semantics.

Schema/storage presence does not count as model authority.
